#!/bin/bash -x

###########################################################
# A script to cd up the directory tree until you reach    #
# the specified folder                                    #
############################################################

function cd..() {
  while [ -n "${PWD##*/}" -a "$1" != "${PWD##*/}" ];
    do cd ..
  done
}
